import React from 'react';
import { useTranslation } from 'react-i18next';

interface Props {}

const MakeAppointmentPhase3: React.FC<Props> = () => {
  const { t } = useTranslation();

  // TODO

  return (
    <div>
      <div>Content, phase 3</div>
    </div>
  );
};

export default MakeAppointmentPhase3;
